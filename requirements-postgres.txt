-r requirements.txt
# psycopg ya viene incluido en requirements.txt (ver comentario ahí). Este
# archivo se conserva por compatibilidad con la documentación existente y
# como instalación explícita para quien prefiera dejar claro que instaló
# soporte de PostgreSQL a propósito.
